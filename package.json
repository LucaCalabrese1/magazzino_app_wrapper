{
  "name": "intime-magazzino-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "APK Android wrapper remoto per /magazzino-app/",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.7.0"
  }
}
